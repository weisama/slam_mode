/**:
  ros__parameters:

    plugin_allowlist:
      - sys_status
      - sys_time
      - command
      - param

      # Offboard 控制
      - setpoint_position
      - setpoint_raw
      - setpoint_velocity

      # 读取 IMU 数据
      - imu

      # 输入外部定位数据（/mavros/vision_pose/pose）
      - vision_pose_estimate

    plugin_denylist: []
